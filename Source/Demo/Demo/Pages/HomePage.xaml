<Page
    x:Class="Demo.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:charting="using:WinRTXamlToolkit.Controls.DataVisualization.Charting"
    xmlns:Series="using:WinRTXamlToolkit.Controls.DataVisualization.Charting"
    xmlns:datavis="using:WinRTXamlToolkit.Controls.DataVisualization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="HomePage_OnLoaded"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="RootGrid" Margin="12,10,12,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Margin="0,0,0,10">
                <TextBlock Text="Chào mừng bạn đến với MoneySaver" Style="{StaticResource SampleHeaderTextStyle}" TextWrapping="Wrap"/>
                <TextBlock Style="{StaticResource ScenarioDescriptionTextStyle}" TextWrapping="Wrap">
                    Tiết kiệm tiền cho bạn
                </TextBlock>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <charting:Chart Name="piechart" Title="PieChart" Height="500" >
                        <charting:Chart.Series>
                            <Series:PieSeries
                            Title="Population"
                            ItemsSource="{Binding Items}"
                            IndependentValueBinding="{Binding Name}"
                            DependentValueBinding="{Binding Value}"
                            IsSelectionEnabled="True" />
                        </charting:Chart.Series>
                    </charting:Chart>
                    <charting:Chart Name="piechart2" Title="PieChart" Grid.Column="1" Height="500">
                        <charting:Chart.Series>
                            <Series:PieSeries
                            Title="Population"
                            ItemsSource="{Binding Items}"
                            IndependentValueBinding="{Binding Name}"
                            DependentValueBinding="{Binding Value}"
                            IsSelectionEnabled="True" />
                        </charting:Chart.Series>
                    </charting:Chart>
                </Grid>
            </ScrollViewer>
            <Pivot Grid.Row="1" Visibility="Collapsed">
                <PivotItem Header="Thu Chi">
                    <charting:Chart Name="piechart3" Title="PieChart" Height="500" >
                        <charting:Chart.Series>
                            <Series:PieSeries
                            Title="Population"
                            ItemsSource="{Binding Items}"
                            IndependentValueBinding="{Binding Name}"
                            DependentValueBinding="{Binding Value}"
                            IsSelectionEnabled="True" />
                        </charting:Chart.Series>
                    </charting:Chart>
                </PivotItem>
                <PivotItem Header="So Sanh">
                    <charting:Chart Name="piechart4" Title="PieChart" Height="500" >
                        <charting:Chart.Series>
                            <Series:PieSeries
                            Title="Population"
                            ItemsSource="{Binding Items}"
                            IndependentValueBinding="{Binding Name}"
                            DependentValueBinding="{Binding Value}"
                            IsSelectionEnabled="True" />
                        </charting:Chart.Series>
                    </charting:Chart>
                </PivotItem>
            </Pivot>
        </Grid>
    </Grid>
</Page>
